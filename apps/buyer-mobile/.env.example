# Expo Go runs on your phone, so localhost cannot reach your Mac.
# For local dev put your Mac's LAN IP here, e.g. http://192.168.1.20:3000/api
# (find it with: ipconfig getifaddr en0)
# For a real build, point this at your deployed API.
EXPO_PUBLIC_API_BASE_URL=
